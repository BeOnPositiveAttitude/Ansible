~/myproj/ansible.cfg
         inventory-dev   #отдельный инвентарь для дев-хостов
         inventory-prod   #отдельный инвентарь для прод-хостов
         site.yml   #общий плейбук с includes
         lamp.yml   #отдельный плейбук для группы хостов lamp
         file.yml   #отдельный плейбук для группы хостов file 
         group-vars/lamp   #переменные для группы хостов lamp
                    file   #переменные для группы хостов file
         host-vars/   #переменные для отдельных хостов

~/.ansible/roles/nginx   #директория с ролями
                 samba
                 nfs
                 httpd

# при работе с Ansible рекомендуется использовать project-директории, чтобы правильно упорядочить контент
# каждая project-директория должна иметь собственный ansible.cfg, инвентарь и плейбуки
# если директория сильно разрастается, могут быть использованы файлы с переменными и другие include-файлы
# также могут быть использованы роли для стандартизации и легкого переиспользования специфических частей Ansible
# роль стоит рассматривать как законченный project выделенный для специфической задачи, которая будет включена в main плейбук

# основные моменты из best practices:
# используйте site.yml в качестве master-плейбука on top in the directory
# из site.yml вызывайте конкретные плейбуки для определенных типов хостов (webservers.yml, dbservers.yml и т.д.)
# учитывайте возможность использования разных инвентарей для разделения между prod и staging фазами
# используйте group_vars/ и host_vars/ для задания переменных относящихся к хостам
# используйте роли для стандартизации распространенных задач