# добавить в файл ~/.vimrc строку, чтобы автоматом было два пробела при нажатии Tab
autocmd FileType yaml setlocal ai ts=2 sw=2 et
ansible-playbook --syntax-check vsftpd.yml   #проверить синтакс плейбука
# использовать параметр -v[vvv] для более детального вывода, максимум четыре "v"
# использовать параметр -C (capital C) для запуска в режиме dry run, когда не происходит реальных изменений

