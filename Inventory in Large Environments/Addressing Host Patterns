# по умолчанию хосты адресуются по их host name как указано в инвентаре
# IP-адреса также могут быть использованы
# хост группы являются общими и определяются в инвентаре
# группа all является неявной и не нуждается в определении
# группа ungrouped также является неявной и адресует все хосты, которые не являются членами какой-либо группы
# wildcards также может использоватсья, - host: '*' приравнивается к - host: all
# если используются специальные символы, всегда помещайте их в кавычки для избежания их интерпретирования шеллом как специальных символов

- hosts: '*.example.com'
- hosts: '192.168.*'
- hosts: 'web*'
- hosts: web1,db1,192.168.4.2
- hosts: web,&eastcoast   #ампресанд означает логическое AND, хост должен быть членом группы web И членом группы eastcoast
- hosts: web,!web1   #член группы web, но не состоит в группе web1
- hosts: all,!web

